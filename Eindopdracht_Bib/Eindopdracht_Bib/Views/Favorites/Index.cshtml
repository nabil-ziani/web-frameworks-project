@model FavoriteViewModel
@if (TempData["Message"] != null)
{
    <div class="alert alert-success" role="alert">
        @TempData["Message"]
    </div>
}

<!-- Pagination -->
<ul class="pagination">
    @for (int i = 1; i < Model.TotalPages + 1; i++)
    {
        <li class="page-item @(i == Model.CurrentPage ? "active" : "")"><a class="page-link" asp-action="Index" asp-route-page="@i" asp-route-sort="@Model.SortField" asp-route-sortDirection="@Model.SortDirection">@i</a></li>
    }
</ul>

<!-- Table with all books -->
<table class="table">
    <thead>
        <tr>
            <th scope="col"><a asp-action="Index" asp-route-sort="ISBN" asp-route-sortDirection="@Model.SortDirection">ISBN Nummer</a></th>
            <th scope="col"><a asp-action="Index" asp-route-sort="Title" asp-route-sortDirection="@Model.SortDirection">Titel</a></th>
            <th scope="col"><a asp-action="Index" asp-route-sort="Author" asp-route-sortDirection="@Model.SortDirection">Auteur</a></th>
            <th scope="col"><a asp-action="Index" asp-route-sort="PublicationYear" asp-route-sortDirection="@Model.SortDirection">Publicatie Jaar</a></th>
            <th scope="col"><a asp-action="Index" asp-route-sort="Type" asp-route-sortDirection="@Model.SortDirection">Type</a></th>
            <th>
                <form method="get" asp-action="Index" asp-route-sort="@Model.SortField">
                    <select asp-for="@Model.SortDirection">
                        <option value="ASC">ASC</option>
                        <option value="DESC">DESC</option>
                    </select>
                    <button type="submit">Change</button>
                </form>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (Favorite favorite in Model.Favorites)
        {
            <tr>
                <td>@favorite.Book.ISBN</td>
                <td>@favorite.Book.Title</td>
                <td>@favorite.Book.Author</td>
                <td>@favorite.Book.PublicationYear</td>
                <td>@favorite.Book.Type</td>
                <td>
                    <button type="button" data-toggle="modal" data-target="#delete-modal-@favorite.Book.Id" class="btn btn-labeled btn-danger">
                        <i class="fas fa-trash"></i>
                    </button>
                    <a asp-action="Update" asp-controller="Books" asp-route-id="@favorite.Book.Id" role="button" class="btn btn-labeled btn-primary">
                        <i class="fas fa-edit"></i>
                    </a>
                    <a asp-action="Add" asp-controller="Favorites" asp-route-id="@favorite.Book.Id" role="button" class="btn btn-labeled btn-primary">
                        <span style="color: yellow;">
                            <i class="fas fa-star"></i>
                        </span>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-controller="Books" asp-action="Create" role="button" class="btn btn-primary">Maak een nieuw boek</a>
<a asp-controller="Books" asp-action="Index" role="button" class="btn btn-primary">Toon alles</a>

<!-- pop up venster om zeker te zijn of je het boek wilt verwijderen -->
@foreach (Favorite favorite in Model.Favorites)
{
    <div class="modal fade" id="delete-modal-@favorite.Book.Id" tabindex="-1" role="dialog" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">@favorite.Book.Title verwijderen?</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">
                    Ben je zeker dat je @favorite.Book.Title wilt verwijderen?
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <a asp-controller="Books" asp-action="Delete" asp-route-id="@favorite.Book.Id" role="button" class="btn btn-danger">Delete</a>
                </div>
            </div>
        </div>
    </div>
}